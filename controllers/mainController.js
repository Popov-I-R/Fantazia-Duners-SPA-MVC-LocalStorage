 let user = (function() {
    let user = new User();
    data.forEach((e) => { e = new Duner(
        e.image,
        e.name,
        e.weight,
        e.category,
        e.price), 
        manager.add(e)
    })
    window.addEventListener("hashchange", router.handleHashChange);
    window.addEventListener("load", router.handleHashChange);
    return user
 })()


    

    